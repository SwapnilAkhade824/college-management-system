package com.college.view.dialogs;

import javax.swing.*;

/**
 * ============================================
 * CLASS: AddMarksDialog
 * ============================================
 *
 * PURPOSE:
 * Popup dialog for user input (Add/Edit operations)
 *
 * RESPONSIBILITIES:
 * - Take input from user
 * - Validate input
 * - Call controller
 *
 * ============================================
 */
public class AddMarksDialog extends JDialog {

    public AddMarksDialog() {
        initUI();
    }

    private void initUI() {
        setSize(400, 300);
        setLocationRelativeTo(null);

        // TODO: Add form fields and buttons
    }
}
