package com.college.controller;

import com.college.model.academic.Course;
import java.util.ArrayList;
import java.util.List;

/** Stub controller — to be implemented when DB is connected. */
public class CourseController {
    public boolean addCourse(Course data)    { return false; }
    public boolean updateCourse(Course data) { return false; }
    public boolean deleteCourse(int id)      { return false; }
    public Course  getCourseById(int id)     { return null; }
    public List<Course> getAllCourses()      { return new ArrayList<>(); }
}
