package com.college.controller;

import com.college.model.academic.Faculty;
import java.util.ArrayList;
import java.util.List;

/** Stub controller — to be implemented when DB is connected. */
public class FacultyController {
    public boolean addFaculty(Faculty data)    { return false; }
    public boolean updateFaculty(Faculty data) { return false; }
    public boolean deleteFaculty(int id)       { return false; }
    public Faculty getFacultyById(int id)      { return null; }
    public List<Faculty> getAllFaculty()       { return new ArrayList<>(); }
}
